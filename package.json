{
  "name": "gater-robot",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {
    "convex": "^1.17.4"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "pnpm": {
    "overrides": {
      "@mysten/dapp-kit": "0.19.11"
    }
  }
}
