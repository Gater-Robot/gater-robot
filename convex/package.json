{
  "name": "@gater/convex",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd .. && pnpm convex dev",
    "deploy": "cd .. && pnpm convex deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "pnpm --filter @gater/chain-registry build && tsc -p tsconfig.test.json && node --test"
  },
  "dependencies": {
    "@gater/chain-registry": "workspace:*",
    "convex": "^1.17.4",
    "viem": "^2.21.54"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "~5.7.2"
  }
}
