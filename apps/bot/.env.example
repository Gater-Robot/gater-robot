# Environment variables for the Telegram bot (`apps/bot`)
#
# Copy to `apps/bot/.env` for local development.

# Required. Telegram bot token from @BotFather.
BOT_TOKEN=

# Required. HTTPS URL for the mini app (dev or prod).
WEBAPP_URL=https://gater-dev.agentix.bot

# Optional. Extra admin lock: if set to a non-empty CSV list, bot admin commands
# are restricted to these Telegram user IDs.
ADMIN_IDS=

# Optional. Bot username (without @). Not required by runtime code today, but
# useful for docs/logs and future features.
BOT_USERNAME=
