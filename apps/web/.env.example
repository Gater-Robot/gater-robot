# apps/web (legacy Vite app) environment template
#
# Copy to `apps/web/.env` for local development.
#
# Note: `apps/webapp` is the v2 migration target; this file exists mainly to make
# env requirements explicit while the legacy app remains in the repo.

VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_WALLET_CONNECT_PROJECT_ID=your-wallet-connect-project-id
VITE_ALCHEMY_API_KEY=your-alchemy-api-key
VITE_BEST_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

