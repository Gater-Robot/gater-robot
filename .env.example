# Root environment template
#
# This file is intentionally comprehensive (monorepo-wide). In practice:
# - Vite apps (`apps/web`, `apps/webapp`) read variables from their own `.env` files
#   or the environment when launched.
# - Convex reads env from the Convex dashboard / `convex env set`, or from the
#   process environment when running locally.
# - The Telegram bot (`apps/bot`) reads from its own `.env` file or the process env.
#
# Tip: also see per-package templates:
# - `apps/webapp/.env.example`
# - `apps/web/.env.example`
# - `convex/.env.example`
# - `apps/bot/.env.example`
# - `packages/contracts/.env.example`

###############################################################################
# Telegram Bot (apps/bot)
###############################################################################

# Required. Telegram bot token from @BotFather.
BOT_TOKEN=your_telegram_bot_token

# Optional. Bot username (without @). Currently not required by runtime code,
# but useful for docs/logs and future features.
BOT_USERNAME=your_bot_username

# Optional extra security layer (recommended when the app grows).
# If set to a non-empty CSV list, secure admin actions are restricted to these
# Telegram user IDs.
# Example: "123456789,987654321"
ADMIN_IDS=

# Required for the bot: the HTTPS URL where the Mini App is hosted.
WEBAPP_URL=https://gater-dev.agentix.bot

###############################################################################
# Webapps (Vite) — apps/web + apps/webapp
###############################################################################

# Required. Convex deployment URL.
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Required (if you want WalletConnect). Obtain from https://cloud.walletconnect.com/
VITE_WALLET_CONNECT_PROJECT_ID=your-wallet-connect-project-id

# Recommended. Used to build Alchemy RPC URLs client-side when present.
VITE_ALCHEMY_API_KEY=your-alchemy-api-key

# Optional. Faucet token contract address (enables `/faucet` write flow).
VITE_BEST_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

###############################################################################
# Convex CLI / deployment (convex)
###############################################################################

# These are used by the Convex CLI (local dev + CI deploy).
# You can also keep them in `convex/.env.local` for local development.
CONVEX_DEPLOYMENT=dev:your-deployment-name
CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_SITE_URL=https://your-deployment.convex.site
CONVEX_DEPLOY_KEY=your-convex-deploy-key

###############################################################################
# Telegram Mini App auth (Convex)
###############################################################################

# Optional. Max age (seconds) for Telegram initData validation.
# Default in code is safe; set explicitly if you need tighter/looser windows.
TELEGRAM_INITDATA_MAX_AGE_SECONDS=86400

# Dev-only. Allow mock initData payloads (`hash=mock`). Useful for browser dev.
ALLOW_MOCK_INITDATA=false

# Recommended for production. When true, legacy (insecure) admin mutations are
# disabled and callers must use secure admin actions.
DISABLE_INSECURE_MUTATIONS=false

###############################################################################
# RPC providers (Convex + Webapp)
###############################################################################

# Recommended. Used by Convex for chain RPC when per-chain overrides are not set.
ALCHEMY_API_KEY=your-alchemy-api-key

# Optional aliases / compatibility.
TELEGRAM_BOT_TOKEN=

# Optional (currently unused in runtime code, kept for future integrations).
ETHERSCAN_API_KEY=
INFURA_API_KEY=

# Optional. Non-Vite WalletConnect id (currently unused; prefer VITE_* for Vite apps).
WALLET_CONNECT_PROJECT_ID=

###############################################################################
# Per-chain RPC overrides (Convex)
###############################################################################

# Convex prefers, in order:
# 1) <CHAINKEY>_RPC_URL (per-chain override)
# 2) ALCHEMY_API_KEY (where supported by our registry)
# 3) viem default public RPC
#
# Chain keys come from `@gater/chain-registry` (SUPPORTED_CHAINS).

MAINNET_RPC_URL=
BASE_RPC_URL=
ARBITRUM_RPC_URL=
POLYGON_RPC_URL=
BSC_RPC_URL=
OPTIMISM_RPC_URL=
BERACHAIN_RPC_URL=
HYPEREVM_RPC_URL=
INK_RPC_URL=
KATANA_RPC_URL=
LENS_RPC_URL=
LINEA_RPC_URL=
MONAD_RPC_URL=
OPBNB_RPC_URL=
PLASMA_RPC_URL=
SONIC_RPC_URL=
UNICHAIN_RPC_URL=
ZKSYNC_RPC_URL=
SCROLL_RPC_URL=
AVALANCHE_RPC_URL=
MANTLE_RPC_URL=
WORLDCHAIN_RPC_URL=
AURORA_RPC_URL=
CELO_RPC_URL=
SHIBARIUM_RPC_URL=
SEPOLIA_RPC_URL=
BASESEPOLIA_RPC_URL=

###############################################################################
# Contracts (packages/contracts)
###############################################################################

# Deployment signer private key (0x...). Keep this in a local `.env`, never commit.
DEPLOYER_PRIVATE_KEY=

# Base RPC endpoint (used for hardhat network config).
BASE_RPC_URL=

# Arc testnet configuration (used only by contracts tooling).
ARC_CHAIN_ID=
ARC_TESTNET_RPC_URL=
