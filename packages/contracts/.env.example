# Hardhat deployment config (`packages/contracts`)
#
# Copy to `packages/contracts/.env` for local development.

# Required for deployments. Private key for the deployer account (0x...).
# Never commit a real key.
DEPLOYER_PRIVATE_KEY=

# Optional. Base RPC endpoint (used by the `base` network in hardhat).
# Example (Alchemy): https://base-mainnet.g.alchemy.com/v2/<key>
BASE_RPC_URL=
# ETH_RPC_URL=$BASE_RPC_URL (Forge uses this when --rpc-url isn’t passed
ETH_RPC_URL=$BASE_RPC_URL

# Arc testnet deployments (optional).
# `ARC_CHAIN_ID` is required when deploying to Arc.
ARC_CHAIN_ID=

# RPC URL for Arc testnet (name matches hardhat config).
ARC_TESTNET_RPC_URL=
# Etherscan API key (v2 — one key works for all chains including Basescan)
ETHERSCAN_API_KEY=

# --- Subscription v4 script/deploy helpers ---
# Foundry script private key and pool manager for v4 subscriptions
PRIVATE_KEY=
POOL_MANAGER=
USDC=
LOCAL_RPC_URL=http://127.0.0.1:8545
DEFAULT_FEE=0
DEFAULT_TICK_SPACING=1
LOCAL_USDC_MINT=2000000000000

# Optional standalone mock deployment script inputs
MOCK_USDC_MINT_TO=
MOCK_USDC_MINT_AMOUNT=1000000000000

# Product creation script defaults
FACTORY=
ROUTER=
HOOK_SALT=
SUB_TOKEN=
SUB_NAME="Gater Subscription Days"
SUB_SYMBOL=subDAYS-GATER
FEE=0
TICK_SPACING=1
SALT_SEARCH_MAX=8000000
BASE_PRICE_USDC=1000000
MONTHLY_BUNDLE_TOKENS=30
MONTHLY_BUNDLE_PRICE_USDC=20000000
YEARLY_BUNDLE_TOKENS=365
YEARLY_BUNDLE_PRICE_USDC=200000000
ENFORCE_MIN_MONTHLY=false
REFUNDS_ENABLED=true
REFUND_PRICE_USDC=100000

# Optional deployment metadata sync for subscriptions.json
SUBS_NETWORK=baseSepolia
SUBS_FACTORY_ADDRESS=
SUBS_ROUTER_ADDRESS=
SUBS_POOL_MANAGER_ADDRESS=
SUBS_USDC_ADDRESS=
SUBS_SAMPLE_TOKEN_ADDRESS=
SUBS_SAMPLE_HOOK_ADDRESS=
